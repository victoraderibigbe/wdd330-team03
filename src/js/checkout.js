import { loadHeaderFooter } from './utils.mjs';

// This line exists so the header and footer show up on the checkout
// page too, the same way we set it up on every other page of the site.
loadHeaderFooter();